package programers;

import java.util.Arrays;
import java.util.stream.IntStream;


public class FindPrime {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		
		/*
		 * //이거보다 더 효율적으로..? 
		 * int n = 10; 
		 * int[] array = IntStream.rangeClosed(2,
		 * n).filter(num -> num == 2 || num % 2 != 0).toArray(); int index = 0;
		 * 
		 * //아 // while(array.length > index) { // int tmp = array[index]; // array =
		 * Arrays.stream(array).filter(num -> num == tmp || num % tmp != 0).toArray();
		 * // // index++; // }
		 * 
		 * 
		 * while (true) { if(index >= Math.sqrt(n)) { break; } index++; }
		 * System.out.println(index);
		 */
		
		
		
		
		
	}
}
